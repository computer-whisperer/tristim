# Build output
target/

# External source (cloned ArgyllCMS for protocol reference; GPL/AGPL, not ours)
refs/

# IDE / editor noise
.vscode/
.idea/
*.swp
*~

# Scratch CSVs at the workspace root — keeps the root clean.
# Committed measurements live under `measurements/`.
/*.csv

# Headless bundle-dump artifacts from the presenter (SVG / tree / draw-ops).
/tristim-gui/out/

# Auto-saved captures from the GUI's in-process capture flow
# (capture-<timestamp>.json, written to the working directory).
capture-*.json
